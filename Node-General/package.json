{
  "name": "knowdev-XXXXXX",
  "version": "0.0.1",
  "description": "XXXXXX",
  "license": "UNLICENSED",
  "author": "Adam Finlayson",
  "scripts": {
    "clean": "npm run test:clean",
    "format": "sort-package-json && eslint --fix",
    "lint": "eslint",
    "postinstall": "npm run postinstall:env && npm run postinstall:husky",
    "postinstall:env": "cpy .env.sample . --rename=.env --no-overwrite",
    "postinstall:husky": "husky install && husky add .husky/pre-commit lint-staged",
    "test": "jest src/",
    "test:clean": "rimraf coverage/",
    "test:coverage": "jest --coverage src/"
  }
}
